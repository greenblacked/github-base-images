version: 2

# Every entry sets the same `cooldown`: do not adopt a version the day it is
# published. If an upstream release is ever compromised, the window between
# publication and discovery is exactly when a same-day bump would pull it in.
#
# `default-days` is the only cooldown key valid here -- the `semver-*-days`
# variants are rejected for both the docker and github-actions ecosystems,
# since neither resolves versions as semver (docker reads a tag, actions read
# a ref).
#
# This delays security fixes by the same amount, which is the real trade-off.
# It is acceptable here because the weekly rebuild plus the Trivy gate already
# catch a vulnerable Debian package without waiting for a base-image bump.

updates:
  # Keeps the action pins in .github/workflows current (docker/*, actions/*,
  # aquasecurity/trivy-action, hadolint, actionlint, zizmor's runner, scorecard).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
    # github/codeql-action is ONE action with three subpaths -- init, analyze
    # and upload-sarif -- pinned to one SHA in six places. Dependabot treats
    # each subpath as its own dependency, so without this it opens three PRs
    # per release, each moving only its own lines.
    #
    # That is not merely noisy, it is broken: CodeQL rejects a setup where init
    # and analyze are on different versions, so two of the three fail CI with
    # "CodeQL job status was configuration error" and the third passes while
    # being a third of a change. It has happened twice here (#32/#33/#34, and
    # the round before it), costing six PRs that all had to be closed and
    # replaced by a hand-assembled one (#36, #37).
    #
    # Grouping makes Dependabot open a single PR moving all six pins together,
    # which is the only arrangement that works.
    groups:
      codeql-action:
        patterns:
          - "github/codeql-action*"

  # Base image bumps arrive as PRs. Dependabot reads the default of the
  # `ARG BASE_IMAGE` each Dockerfile builds FROM, which is why that default
  # stays pointed at the real upstream ref rather than the mirror.
  #
  # Note this does NOT track the pinned tool versions in ci-tools/Dockerfile.ci
  # (Terraform, kubectl, AWS CLI, Docker client) -- those are plain ARGs with no
  # ecosystem behind them, so they move when a human moves them.
  - package-ecosystem: docker
    directory: /ci-node22
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-python313
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-go
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-tools
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-rust
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-ruby34
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-java21
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  # Does not track COMPOSER_VERSION -- that is a plain ARG, like the ci-tools pins.
  - package-ecosystem: docker
    directory: /ci-php84
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  # Base lives on mcr.microsoft.com; Dependabot handles non-Hub registries.
  - package-ecosystem: docker
    directory: /ci-dotnet9
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-node24
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-python312
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-java17
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-dotnet8
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-cloud
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-security
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-db
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  # Base lives on mcr.microsoft.com; Dependabot handles non-Hub registries.
  - package-ecosystem: docker
    directory: /ci-dotnet10
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-java25
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-python314
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  # Does not track COMPOSER_VERSION -- that is a plain ARG, like the ci-tools pins.
  - package-ecosystem: docker
    directory: /ci-php85
    schedule:
      interval: weekly
    cooldown:
      default-days: 7

  - package-ecosystem: docker
    directory: /ci-ruby40
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
